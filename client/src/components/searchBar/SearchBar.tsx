import { Input } from "semantic-ui-react"

const SearchBar = () => {
    return <>
        <Input size='small' icon='search' placeholder='Search...' style={{ width: '80%' }} />
    </>
}

export default SearchBar